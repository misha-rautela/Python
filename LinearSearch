#Search for an element  using linear search
#suppose the searching element not present in the error then return -1
#Time Complexity : O(n)
#Space Complexit : O(1)

#Function Definition:
def linearsearch(arr,x):
    for i in range(len(arr)):
        if(arr[i]==x):
            return i
    return -1

#Driver Code
arr=[20,40,70,10,15,18,29,75,46]
print("Here's the array you want to search an integer \n")
x=int(input("Enter the element you want to search"))
#Function Calling
result=linearsearch(arr,x)

print("Searched element is present at the index", result)
